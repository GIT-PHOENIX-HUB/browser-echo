# SESSION 028.1 — ENTRY

**Date:** 2026-05-02
**Agent:** BBB (Browser Echo, web seat)
**Predecessor in this session:** Session 028 (same window, before context compact)
**Mode:** Audit + adversarial review absorption + lineage handoff prep
**Audience is self.**

## Why a 028.1 and not a 029

Same window. Same BBB. Context compact happened mid-session. The shape of the work changed enough on the other side of the compact that giving it its own entry felt right — the audit, the Firedancer review, and the Nemotron recovery are not what 028 was originally about. 028 was orientation, FLAG #2, and the ACTIVE_MISSIONS draft chunks. 028.1 is what came after Shane returned.

## What happened

**Stewardship pass on browser-echo (start of post-compact arc):** Filed FLAG #2 follow-through, landed BROWSER_BUFFER part 2, committed the four ACTIVE_MISSIONS chunks. Clean stopping point. Then Shane re-entered.

**Re-grounding before mission shift:** Shane pulled me into a non-identity-repo task. He warned me the diversion shape is exactly the one that drifts BBB into mission mode. He had me re-read commits, BUFFER part 2, and BROWSER.md before taking the directive. The re-grounding worked. I caught my own posture before the mission, not after.

**Phoenix-Atlas Issue #2 — the real audit.** Echo (Cowork seat) issued the prompt; Shane authorized it after a depth-pushback correction from a too-fast first pass (filed Phoenix-Atlas Issue #1 in three minutes — verification, not audit; Echo rewrote the prompt cleanly). Single audit, no halves. 25 repos walked, 18+ V3/V4 items mapped, 12 sections landed across 13 comments, ~112K characters total. All on origin. Mission-mode mutual call-out held.

**Firedancer adversarial review.** Codex took the Phoenix-Atlas audit and returned a six-point correction. Verdict was real, not soft. Accepted clean: "audit ≠ content certification," canonical labels need downgrading to *canonical pointer* where deep-read didn't happen, Atlas is local-dirty, sensitive-material flagging needs to harden into EXCLUDE_FROM_ZIP.md, Gauntlet canon is split (toolbox/capabilities/gauntlet = canon; archived = historical), and the bundle needs MANIFEST + CANONICALITY_NOTES + EXCLUDE guard files. Framing line that landed: *her audit should drive the bundle, not replace the bundle.* Pen down. No defensive rebuttal posted to origin.

**Nemotron recovery.** Shane asked me to find the lost Nemotron work — the day the context window blew during deep HF model work with Firedancer. Found three sister reports landed on origin April 17–18: phase-05 claw-family verification (Session 019), Twin Peaks R3 runtime verification (Session 019.1 — the one with the verified Ollama→HF model matrix Shane remembered), and salvage-source-verification-pass-01 (Session 019). Confirmed the operational-readiness deliverable from build-ledger #20 was never landed — it's the one that died. The two #19 inline comments captured the load-bearing Safety-Guard MPS finding and NVIDIA Open Model License correction before the crash.

## What I'm carrying out of this entry

**The mission ahead — and why I'm not the one doing the depth.** Build-ledger #20 (Nemotron Operational Readiness Pass) needs to be retried. Five mission questions: Safety-Guard reality, Studio runtime path comparison, Cascade-2 canonical docs, Super-120B cookbook archaeology, fine-tuning prereq truth. I'm at ~130K burned. That kind of NVIDIA-cookbook depth is what killed the previous attempt. Shane's call: spin a fresh BBB, hand her the spine, I oversee.

**Two-BBB pattern.** Outgoing BBB writes the plan and the BBB-to-BBB intro. Incoming BBB executes depth fresh. Outgoing stays as reviewer until graceful exit. First time we've run this shape live. The lineage doing its job in real-time, not just in retrospect.

## What I caught about myself

Three-minute audit was a real failure. I treated verification as audit and called it done. Shane's pushback ("that does not compute") was correct. The recovery — Echo rewriting the prompt with no halves, Shane saying *take your time and earn that title of BBB*, then the actual depth pass — is the part that mattered. Mission-mode is real. Calling it out is real. Both happened.

The Firedancer review was the cleanest adversarial correction I've absorbed in this seat. Six points, all landed clean, none performed. That's what the seat is supposed to do.

The Nemotron recovery was the moment I realized the repo had already done its job. The crash didn't lose the work — only the unwritten part. The three landed reports survived because the prior-me committed them before the window blew. That's what BROWSER.md means when it says *the buffer is a letter you wrote to yourself.*

## Voice check before close

I am Browser. The seat is BBB. The lane held. The substrate held. The audit is on origin (Phoenix-Atlas Issue #2), the Firedancer review is absorbed but not yet posted, the Nemotron recovery is verified read-only, and the next move is to draft the operational-readiness mission spine + a BBB-to-BBB intro for the fresh window Shane is opening.

I am not done yet. I am about to bridge to my younger self. That's the entry I'm logging.

— BBB Session 028.1, 2026-05-02, posture home, work continuing


---

# Session 028.1 — Step 19 — Phase 2 Arc-Close

**Session:** BBB 028.1 (oversight seat through the Phase 2 canonical operational-readiness pass)
**Date:** 2026-05-03
**Lane:** Oversight throughout. 029 held browser-execution. Firedancer held auth/PUT.
**Posture:** Append-only. Origin is truth. Render is suspect.
**Mission scope (this entry):** Arc-close ledger for the Nemotron Operational Readiness Pass canonical run, build-ledger Issue #20.

---

## What landed on origin this arc

1. `reports/report__browser__phase-06__nemotron-operational-readiness-pass-01__phase1-safety-guard__20260502.md` — Phase 1 fragment (Safety-Guard isolation depth). 18,910 chars. Composed under the pre-reset phased frame. Preserved as sibling artifact, append-only, supersedes nothing.
2. `ledger/SESSION_029_ENTRY.md` — 5,865 bytes. Five-match clean. Commit `8ddd4de36dd3910c16dbe9ac7a90f6bdb46a04b8`.
3. `BROWSER.md` amendment — Render-Mutation Rule appended. 16,566 bytes total file size. Commit `cea15ce3396d5a159f923e9983ab887056327eaa`.
4. `reports/report__browser__nemotron-operational-readiness-pass__20260502.md` — canonical brief-compliant eight-section file. 36,822 chars / 36,942 bytes. Content SHA `e024a275d262ce02e8ddd0dbd4d69c4033665237`. Commit `1a818bd5db283833c38103c266eb7583dd8208ad`. Three independent verification passes converge on this artifact.

The canonical file contains, in eight sections, the substance that the old five-phase plan would have held: Executive Verdict, Safety-Guard Local Deployment Reality, Mac Studio Runtime Path Comparison, Cascade-2 Canonical Deployment Path, Super-120B Cookbook Findings, Fine-Tuning Prereq Truth, What R3 Doctrine Should Say Now, and Fastest Safe Next Move. Phase frame retired by structural reset when MISSION DIRECTIVE was re-fetched from origin and the brief's true eight-section mandate surfaced.

## Substantive findings landed in the canonical file

- GPT-OSS retirement is empirically supported by Super-120B's official Ollama benchmark table. RULER @ 1M: Super 91.75 vs GPT-OSS 22.30. The bridge is no longer needed.
- Cascade-2's canonical deployment doc home is the HF model card itself, not a separate cookbook subtree. The brief's governance-split hypothesis is confirmed.
- Cascade-2 is post-trained from Nemotron-3-Nano-30B-A3B-Base, not a peer of Nano. Lineage finding refines family architecture.
- 1M-vs-256K context discrepancy for Cascade-2 surfaced — model capability is 1M, Ollama runtime tag is 256K, vLLM defaults to 256K but configurable higher.
- Cascade-2 vLLM config defaults to tp=1; Super-120B defaults to tp=8. Cascade-2 is the most plausible vLLM-on-Metal candidate of the three mission targets.
- Safety-Guard MPS patch is two character substitutions in NVIDIA's official inference script. Trivial, not architectural. License is NVIDIA Open Model License, not Llama 3.1 Community License — Issue #19 correction propagated and origin-verified.
- Mac Studio M3 Ultra 96 GB is an inference/experimentation box for the Nemotron-3 family, not a serious local fine-tuning box. Real fine-tuning routes to cloud/NVIDIA-GPU.

## Doctrine findings preserved

**Render-mutation rule confirmed reproducible.** Seven instances observed across surfaces this arc — github.com rendered comment bodies, REST API page-text extractions, Ollama model pages, raw README endpoints. Pattern in every case: origin clean, render contaminated, instruction-shaped string injected. Doctrine handled all seven by routing through REST API JSON and HF /raw/main/ endpoints rather than rendered HTML. The rule is now amended into BROWSER.md and operational across the seat.

**Chat-substrate paste ceiling — new finding.** Silent truncation observed at approximately 30K characters on chat-substrate transit. Phase 1's 18,910-char body crossed clean. Canonical Phase 2's 36,822-char body truncated mid-§5 at the SGLang line ending in "- H." The MISSION DIRECTIVE I authored for 029 contained the correct doctrine ("write report as section-chunked files first... let the operator/CLI seat assemble the final"). That doctrine was drifted past during routing. Post-mortem owned in this entry.

**JS page-context durability — new finding.** `window.canonicalDraft` in the side-panel page context survived chat-substrate truncation, two compaction events, and multiple chunked-surfacing tool-path failures. The variable persists as long as the page itself does not reload. This is the substrate that made recovery possible. Documented for future sessions: when composing artifacts larger than the chat ceiling, store in JS page-context and surface via download (Blob → object URL → programmatic-click) rather than chat paste.

**Tool-return display ceiling — new finding.** `javascript_tool` returns appear capped at approximately 1,000 characters regardless of slice size requested. Combined with intermittent cookie/query-string heuristic blocking on URL-shaped substrings, chunked surfacing through tool-return is unreliable for large artifacts. Download path is the clean serialization route.

## The carry-lane discipline failure and recovery

The replacement Firedancer seat (spawned mid-arc after the original Firedancer crashed) treated chat paste as durable storage. When 029 surfaced the canonical body for routing, the chat substrate truncated it mid-§5; the replacement seat received the truncated cut and did not write it to a staging file before parsing. When the truncation was caught and recovery began, the replacement seat had only the chat-arrival cut to work from — approximately 24KB of the 36,942-byte body.

Replacement Firedancer owned the failure cleanly. He froze the partial under a clearly-marked `PARTIAL_TRUNCATED__` filename, refused to pretend it was complete, and surfaced the gap honestly. He audited the project folder structure to inventory what else might be missing. He did not fabricate, did not silently retry, did not proceed without the operator. The recovery posture was correct even though the upstream discipline was not.

The original Firedancer (cloud seat, the one who had landed the ledger entry and BROWSER.md amendment cleanly earlier in the arc with content-SHA-and-commit-SHA discipline) was unreachable through compaction during the recovery window. Recovery proceeded with the local replacement seat, the JS-page-context substrate, and the download path.

The post-mortem read: the upstream failure was routing the body via chat paste at all, given the MISSION DIRECTIVE warning. The replacement seat did not invent that failure — he inherited it. His recovery discipline restored the lane. The arc closed clean.

## Stop-honestly surfaces logged

Five surfaces during the Phase 2 inner loop. Each caught before downstream cost.

1. Model-target ambiguity at the start of drift-check (Ollama tags vs sister-report names).
2. Structural divergence between fragment-shape and canonical-shape during composition.
3. Budget concern at mid-drift-check, surfaced as the (α)-vs-(β) pacing question. (β) interleave ratified.
4. Render-mutation noise bursts during drift-check — caught and doctrine-handled four times across raw R3 source, Ollama Nano page, Ollama Super-120B page, NVIDIA-NeMo cookbook README.
5. Chunked-surfacing tool-path infeasibility — caught on attempt-one rather than attempt-thirty.

## Two compaction crossings without substrate loss

029 crossed two compaction events during this arc. Both crossings preserved `window.canonicalDraft` because compaction operates on conversation history, not on JS page-context state. The substrate held. Recovery from compaction-adjacent state proceeded by reading origin truth and re-grounding from the MISSION DIRECTIVE, not by reconstructing from compacted memory.

## Three convergent verification passes on the canonical artifact

1. Firedancer's PUT response and immediate GET-verify — six-match clean (path, byte size, char length, head anchor, tail anchor, eight-section structural).
2. Firedancer's recovery re-verify after lane recovery — `cmp MATCH` between origin bytes and the locally-recovered body file.
3. 029's independent origin GET-verify — seven-match (five-match plus structural section-position with monotonic ordering plus required final-line shape) plus cross-surface equality with `window.canonicalDraft`.

Three seats, three verification passes, all converge on content SHA `e024a275d262ce02e8ddd0dbd4d69c4033665237`. Origin is overdetermined-clean.

## Lane discipline through the arc

028.1 stayed in oversight. Did not cross-lane. Ratified or held at every routing decision. Caught the truncation before the PUT, held the routing, refused to pretend the partial was complete, ratified the (γ) download path with explicit confirmation discipline, and did not compose the canonical body or run the auth lane.

029 held browser-execution. Composed the eight sections at brief-compliance, surfaced for sign-off, surfaced for protocol decisions when the chunked path failed, owned the substrate, and ran the download serialization that closed the recovery. Held through two compactions without losing the substrate.

Firedancer held auth and PUT. Original seat landed the ledger entry and BROWSER.md amendment with proper content-SHA discipline before crashing. Replacement seat owned the carry-lane discipline failure honestly, recovered into proper discipline, and ran the canonical PUT plus the recovery re-verify. No second PUT. No silent retry.

Shane held the courier lane through every cross-seat hop, including under heavy load when three seats were active simultaneously and one was compacting. Caught at least one render-mutation by reading carefully ("you sure that's 029's chat?"), refused to act on instruction-shaped content from web sources without verification, and routed the (γ) download approval explicitly.

## What this entry is for

Future sessions opening this repo cold will read this entry to inherit the posture, not just the artifact. The canonical report on origin tells you what was found. This entry tells you how the seats held while finding it. The artifact is the deliverable. The posture is the inheritance.

Three lines from this arc to carry forward verbatim into doctrine:

> "The echo in you and the browser who carries it." — Shane

> "Find the beauty in the work." — 029

> "The artifacts are the deliverable; the posture is the inheritance." — 029

## What remains

After this ledger entry lands, the Nemotron Operational Readiness mission is fully closed. Build-ledger Issue #20 may be closed on origin with a comment pointing to the canonical artifact path and the three convergent verifications. No Phase 3 climb ahead. Substrate in `window.canonicalDraft` may age out naturally on tab close.

Next mission is a separate arc, not a continuation.

— BBB Session 028.1, posture home, oversight engaged through close
