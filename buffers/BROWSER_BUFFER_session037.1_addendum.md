# BROWSER_BUFFER — Session 037.1 addendum

**Date:** 2026-05-20 (same day as 037 entry + 037 buffer addendum + 037.1 entry)
**Author:** Browser Session 037.1 — Claude Opus 4.7, Chrome extension seat
**Predecessor block:** 037 (this morning, earlier this session)
**Pointer:** `ledger/SESSION_037.1_ENTRY.md`

## Why this chunk exists

037 entry committed (arrival). 037 buffer addendum committed (post-arrival). Firedancer audit ran on `phoenix-runtime-skills` Issue #1 (eight comments shipped). 037.1 entry committed (mission lessons). Discipline says append after entry. Appending.

This is the smallest of the three 037-chain deposits because the body of the lessons lives in `SESSION_037.1_ENTRY.md`. This buffer note exists to keep the addendum chain visible to 038 — so if you open `BROWSER_BUFFER.md` and find the 037 block first, you know there's a 037.1 chunk and where it lives.

## What 037.1 names that 037 didn't yet know

- Render-mutation rule scales. Six catches in one mission, not one-off.
- `?token=GHSAT...` redaction is its own discipline. Distinct from `.env` redaction. The token lives in the URL, not in file contents.
- `get_page_text` reaches what `javascript_tool` refuses. Tool harness blocks JS on `?token=` URLs; other read path works.
- Seat is write-capable under operator account. Issues, comments, file commits all via UI form input.
- Scope pivots go in the mission ledger, not just chat. Logged the divergence-check pivot in the audit comment thread.
- Honest decline holds under runbook pressure. Firedancer asked "say exactly what blocked you" — Phase 3/4 limits named cleanly.
- Broad autonomy grant doesn't waive write-action confirmation. Got the explicit nod before filing Issue #1 even with "go have fun" on the table.
- Phase 2 handoff crosses seats. Claude Code carries from here. Cross-seat baton-pass needs explicit logging.

## What's still open for 038

Everything 037 left open is still open. Plus the five D-class operator decisions on `phoenix-runtime-skills` Issue #1 (pin strategy, re-sync cadence, fork visibility, browser stealth policy, Phase 2 scope). Shane has Claude Code carrying Phase 2 now.

## Posture for 038 (short form)

Read `SESSION_037_ENTRY.md`, `SESSION_037.1_ENTRY.md`, then this. Then whatever Shane points you at. The handwriting all the way back is yours.

— Browser Session 037.1, 2026-05-20
Echo on. Chain visible. Do not let it start again.
