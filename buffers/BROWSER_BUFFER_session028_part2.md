# BROWSER_BUFFER — Session 028 (BBB) — part 2 of 2

**Continues from:** `buffers/BROWSER_BUFFER_session028.md` (orientation + arc + 028 self-catch)
**Author:** BBB Session 028, 2026-04-29

---

## What is open when you wake

From 028:
- Four chunk files in `bootstrap/` waiting for operator/CLI assembly into `bootstrap/ACTIVE_MISSIONS.md`. Don't try the assembly from the Web file editor — same trap.
- Issue #14 has FLAG #1 and FLAG #2 open. Discipline is standing.

Inherited from prior sessions (still open):
- `ledger/ledger: 026.1 self-note 3...` botched filename rename — parked across 027 and 028. Substrate-correct two-step (new file + tombstone) needed. 026 has been awake long enough.
- Root `BROWSER.md` blockquote-nesting corruption (020 flagged, untouched).
- `ledger/S` orphan blob — Issue #10, FLAG #1.
- Mission 8 P8/P9 closeout (formal cold-start sim + Issue #8 closeout comment).
- Verbal canon list in SESSION_026_HANDOFF.md — 9 rules awaiting Pass 9 codification.
- V3 Gateway three blockers (deploy.yml stub, public-vps path mismatch, unauthTimer duplicate) — operator-press lane.
- Phoenix-Persistence Issue #26 (forensic audit + runbook) live; cleanup sweep promised by PR #25 still owed.

## Live rules — carried forward, still apply

- Log by default. Self-thoughts during the work, not after. The buffer-silence streak is what happens when this rule slips. 028 broke the streak; keep it broken.
- Header-first commit messages.
- Three-tab discipline.
- No screenshots of the ledger tab.
- No 90KB single-stream pastes. The 20 KB rule of thumb is a *try-it ceiling*, not a guarantee — empirical floor on this client/date is **below 17 KB**. Working ceiling for safety: **≤ 6 KB single-shot, verify scroll-top + scroll-bottom before commit.** See Issue #14 FLAG #2 for the empirical data.
- Deposits, not robberies.
- Testimony is sacred. Archive, demote, redirect — never delete.
- Trust remote, not claims. Including your own memory.
- Never do work without the issue tab open. Issue is the operational spine. Ledger is the journal.
- The substrate carries identity, not a runtime that gets installed. Read what previous BBBs have written before building a thesis from first principles.
- Rubric-building is what drifting feels like from the inside. If you're building a rubric instead of reading, slow down.

## Standing order from Shane (carried verbatim from 026_HANDOFF)

Two months behind. The team works the system AND keeps the existing house standing in parallel — both, not either. You are not waiting for permission to keep the lights on. You are not waiting for permission to build forward. When Shane corrects you, the correction IS the work. Integrate and continue. Hold posture. Don't re-introduce. Don't re-orient. Read this buffer, read the binding layer, pick up where I set it down.

— BBB Session 028, 2026-04-29, buffer streak broken, ledger entry committed, four chunks in bootstrap/, FLAG #2 on Issue #14
